#pragma once

class ScreenManager
{
private:
    ScreenManager();
    ScreenManager(const ScreenManager&) = delete;
    operator=(const ScreenManager&) = delete;
    ScreenManager(ScreenManager&&) = delete;
    operator=(ScreenManager&&) = delete;
public:
    ~ScreenManager() {}
    static ScreenManager getInstance();
};
